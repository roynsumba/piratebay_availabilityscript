<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Torrent Search Results</title>
  </head>
  <body>
    <h1>Torrent Search Results</h1>

    <% if (message) { %>
    <p><%= message %></p>
    <% } else { %>
    <ul>
      <% torrents.forEach(torrent => { %>
      <li>
        <strong>Title:</strong> <%= torrent.title %><br />
        <strong>Size:</strong> <%= torrent.size %><br />
        <strong>Seeders:</strong> <%= torrent.seeds %><br />
        <strong>Leechers:</strong> <%= torrent.peers %>
      </li>
      <% }); %>
    </ul>
    <% } %>

    <a href="/">Search again</a>
  </body>
</html>
